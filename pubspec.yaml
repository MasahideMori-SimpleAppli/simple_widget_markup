name: simple_widget_markup
description: This is an option that allows you to statically create layouts.
version: 43.0.2
repository: https://github.com/MasahideMori-SimpleAppli/simple_widget_markup

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1
  simple_block_markup_language: ^3.3.0
  simple_managers: ^7.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0