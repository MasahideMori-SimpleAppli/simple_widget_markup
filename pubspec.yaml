name: simple_widget_markup
description: This is an option that allows you to statically create layouts.
version: 30.0.0
repository: https://github.com/MasahideMori-SimpleAppli/simple_widget_markup

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.20.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.10
  simple_block_markup_language: ^3.1.0
  simple_managers: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter