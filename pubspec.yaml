name: simple_widget_markup
description: This is an option that allows you to statically create layouts.
version: 19.0.0
repository: https://github.com/MasahideMori-SimpleAppli/simple_widget_markup

environment:
  sdk: ">=2.15.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.10
  simple_block_markup_language: ^2.0.0
  textfield_manager: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter