name: simple_widget_markup
description: The simple widget markup language converter. Easily create text and screen layouts.
version: 1.3.1
repository: https://github.com/MasahideMori-SimpleAppli/simple_widget_markup

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter